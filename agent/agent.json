{
  "$comment": "TrueForge agent spec (AgentSpecSchema). Loaded by the harness at npx @truefoundry/trueforge (localhost:8790). The three mcp_servers are registered as connectors in TrueForge Settings: `github` and `bright-data` ship in TrueForge's catalog (paste a token); `sentinel` is this repo's custom MCP server (sentinel-mcp/). See agent/README.md for the load steps and agent/instructions.md for the full four-stage orchestrator prompt (Discover/Inspect/Judge/Act) — the harness inlines that file's text into the `instructions` field below at deploy time.",
  "model": { "name": "REPLACE_WITH_MODEL" },
  "instructions": "See agent/instructions.md — the harness inlines the full text of that file into this field at deploy time.",
  "mcp_servers": [
    {
      "name": "github",
      "enable_tools": ["@all"],
      "require_approval_for_tools": ["@write", "@destructive"]
    },
    {
      "name": "bright-data",
      "enable_tools": ["@all"],
      "preload": true
    },
    {
      "name": "sentinel",
      "enable_tools": ["@all"]
    }
  ],
  "config": {
    "dynamic_sub_agents": { "enabled": true },
    "sandbox": { "enabled": true },
    "ask_user_questions": { "enabled": true },
    "generative_ui": { "enabled": true }
  }
}
